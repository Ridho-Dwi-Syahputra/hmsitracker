<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <%- include('../partials/navbarHMSI', { user: user, unreadCount: unreadCount }) %>
  <%- include('../partials/sidebarHMSI', { user: user, activeNav: 'laporanSelesai' }) %>

  <main class="lg:ml-80 px-6 pt-20 pb-8 flex-grow">
    <div class="max-w-6xl mx-auto bg-white shadow-xl rounded-2xl overflow-hidden border border-orange-200">

      <div class="bg-orange-500 text-white px-6 py-6">
        <h1 class="text-2xl font-bold">Laporan Diterima</h1>
        <p class="text-sm text-orange-100">Arsip laporan program kerja yang telah disetujui oleh DPA</p>
      </div>

      <div class="p-6 sm:p-8">
        <% if (!laporanSelesai || laporanSelesai.length === 0) { %>
          <div class="text-center py-20">
            <i class="fa-solid fa-check-double text-4xl text-gray-300 mb-4"></i>
            <p class="text-xl font-semibold text-gray-800">Belum Ada Laporan Diterima</p>
            <p class="text-gray-500">Laporan yang sudah disetujui DPA akan muncul di sini.</p>
          </div>
        <% } else { %>
          <div class="overflow-x-auto">
            <table class="min-w-full text-sm text-center">
              <thead class="bg-orange-100 text-gray-700">
                <tr>
                  <th class="py-3 px-4 font-semibold">No</th>
                  <th class="py-3 px-4 font-semibold text-left">Judul Laporan</th>
                  <th class="py-3 px-4 font-semibold">Program Kerja</th>
                  <th class="py-3 px-4 font-semibold">Tanggal Disetujui</th>
                  <th class="py-3 px-4 font-semibold">Aksi</th>
                </tr>
              </thead>
              <tbody>
                <% laporanSelesai.forEach((l, index) => { %>
                  <tr class="hover:bg-orange-50 transition">
                    <td class="py-3 px-4 border-b"><%= index + 1 %></td>
                    <td class="py-3 px-4 border-b text-left font-medium"><%= l.judul_laporan %></td>
                    <td class="py-3 px-4 border-b"><%= l.Nama_ProgramKerja %></td>
                    <td class="py-3 px-4 border-b text-gray-500"><%= l.tanggalFormatted %></td>
                    <td class="py-3 px-4 border-b">
                      <a href="/hmsi/laporan-selesai/<%= l.id_laporan %>/detail" class="text-orange-500 hover:text-orange-700" title="Lihat Detail">
                        <i class="fa-solid fa-eye text-lg"></i>
                      </a>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>