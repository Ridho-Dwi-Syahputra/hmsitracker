<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/images/HMSI_Tracker.png">
  <link rel="apple-touch-icon" href="/images/HMSI_Tracker.png">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- =====================================================
       MODAL KONFIRMASI PERTAMA
       Modal untuk konfirmasi awal perubahan status
       ===================================================== -->
  <div id="confirmModal1" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center hidden">
    <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all duration-300 scale-95 opacity-0" id="confirmModal1Content">
      <div class="text-center">
        <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-2">Konfirmasi Tindakan</h3>
        <p class="text-gray-600 mb-6">
          Apakah Anda yakin ingin menandai program kerja ini sebagai <strong id="statusTarget1" class="text-orange-600"></strong>?
        </p>
        <div class="flex gap-3">
          <button id="cancelConfirm1" class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 font-medium py-3 px-6 rounded-xl transition-colors duration-200">
            Batal
          </button>
          <button id="proceedConfirm1" class="flex-1 bg-orange-500 hover:bg-orange-600 text-white font-medium py-3 px-6 rounded-xl transition-colors duration-200">
            Ya, Lanjutkan
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- =====================================================
       MODAL KONFIRMASI KEDUA
       Modal untuk validasi teks sebelum perubahan final
       ===================================================== -->
  <div id="confirmModal2" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center hidden">
    <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all duration-300 scale-95 opacity-0" id="confirmModal2Content">
      <div class="text-center">
        <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-2">Tindakan Tidak Dapat Dibatalkan</h3>
        <p class="text-gray-600 mb-4">
          Setelah program kerja ini dinyatakan <strong id="statusTarget2" class="text-red-600"></strong>, statusnya tidak dapat diubah kembali. Apakah Anda benar-benar yakin ingin melanjutkan?
        </p>
        
        <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-4 text-left">
          <p class="text-sm text-yellow-700">
            <i class="fa-solid fa-exclamation-triangle mr-2"></i>
            Ketik <strong>YA, SAYA YAKIN</strong> untuk melanjutkan
          </p>
        </div>

        <input 
          type="text" 
          id="validationInput" 
          placeholder="Ketik YA, SAYA YAKIN"
          class="w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:border-red-500 focus:outline-none transition-colors duration-200 mb-4"
          autocomplete="off"
        />
        
        <div id="validationError" class="text-sm text-red-600 mb-4 hidden">
          <i class="fa-solid fa-times-circle mr-1"></i>
          <span>Teks tidak sesuai! Mohon ketik dengan benar.</span>
        </div>

        <div class="flex gap-3">
          <button id="cancelConfirm2" class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 font-medium py-3 px-6 rounded-xl transition-colors duration-200">
            Batal
          </button>
          <button id="proceedConfirm2" class="flex-1 bg-red-500 hover:bg-red-600 text-white font-medium py-3 px-6 rounded-xl transition-colors duration-200">
            Validasi & Ubah
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- =====================================================
       MODAL SUCCESS
       Modal untuk menampilkan pesan sukses
       ===================================================== -->
  <div id="successModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center hidden">
    <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all duration-300 scale-95 opacity-0" id="successModalContent">
      <div class="text-center">
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        <h3 id="successModalTitle" class="text-xl font-bold text-gray-900 mb-2">Status Berhasil Diubah!</h3>
        <p id="successModalMessage" class="text-gray-600 mb-6">Status program kerja telah berhasil diperbarui.</p>
        <button id="closeSuccessModal" class="w-full bg-green-500 hover:bg-green-600 text-white font-medium py-3 px-6 rounded-xl transition-colors duration-200">
          Mengerti
        </button>
      </div>
    </div>
  </div>

  <!-- =====================================================
       MODAL ERROR
       Modal untuk menampilkan pesan error
       ===================================================== -->
  <div id="errorModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center hidden">
    <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all duration-300 scale-95 opacity-0" id="errorModalContent">
      <div class="text-center">
        <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </div>
        <h3 id="errorModalTitle" class="text-xl font-bold text-gray-900 mb-2">Gagal Mengubah Status!</h3>
        <p id="errorMessage" class="text-gray-600 mb-6">Terjadi kesalahan saat mengubah status.</p>
        <button id="closeErrorModal" class="w-full bg-red-500 hover:bg-red-600 text-white font-medium py-3 px-6 rounded-xl transition-colors duration-200">
          Tutup
        </button>
      </div>
    </div>
  </div>

  <!-- =====================================================
       MODAL LOADING
       Modal untuk menampilkan proses loading
       ===================================================== -->
  <div id="loadingModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center hidden">
    <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full mx-4 transform transition-all duration-300 scale-95 opacity-0" id="loadingModalContent">
      <div class="text-center">
        <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <div class="w-8 h-8 border-4 border-blue-200 border-t-blue-500 rounded-full animate-spin"></div>
        </div>
        <h3 id="loadingModalTitle" class="text-xl font-bold text-gray-900 mb-2">Memproses...</h3>
        <p id="loadingModalMessage" class="text-gray-600 mb-6">Mohon tunggu sebentar.</p>
      </div>
    </div>
  </div>

  <!-- =====================================================
       NAVBAR DAN SIDEBAR
       Include navigasi dan sidebar dari partials
       ===================================================== -->
  <%- include('../partials/navbarDPA', { user: user, unreadCount: typeof unreadCount !== 'undefined' ? unreadCount : 0 }) %>
  <%- include('../partials/sidebarDPA', { activeNav: activeNav, user: user }) %>

  <!-- =====================================================
       MAIN CONTENT AREA
       Konten utama halaman detail program kerja
       ===================================================== -->
  <main class="lg:ml-80 px-6 pt-20 pb-8 flex-grow">
    <div class="max-w-5xl mx-auto bg-white shadow-xl rounded-2xl overflow-hidden border border-gray-200">

      <!-- Header Section -->
      <div class="bg-gradient-to-r from-orange-500 to-orange-600 text-white px-6 py-6 flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold">Detail Program Kerja</h1>
          <p class="text-sm text-orange-100">Informasi lengkap program kerja HMSI</p>
        </div>
      </div>

      <!-- Body Content -->
      <div class="p-6 sm:p-8 space-y-10">

        <!-- =====================================================
             REUSABLE COMPONENT: boxField()
             Helper function untuk menampilkan field dengan konsisten
             ===================================================== -->
        <%
        function boxField(label, value) {
          const displayValue = value || '-';
        %>
          <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
            <p class="font-medium text-gray-600 mb-2"><%= label %></p>
            <div class="bg-white border border-gray-200 p-3 rounded-lg">
              <p class="text-gray-800 whitespace-pre-line"><%= displayValue %></p>
            </div>
          </div>
        <% } %>

        <!-- =====================================================
             SECTION: INFORMASI DASAR
             Menampilkan informasi utama program kerja
             ===================================================== -->
        <section>
          <h4 class="text-lg font-semibold text-gray-700 mb-4">Informasi Dasar</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="md:col-span-2">
              <%= boxField("Nama Program Kerja", proker.namaProker) %>
            </div>
            <%= boxField("Tanggal Mulai", proker.tanggalMulaiFormatted) %>
            <%= boxField("Tanggal Selesai", proker.tanggalSelesaiFormatted) %>
            <%= boxField("Divisi", proker.divisi) %>
            <%= boxField("Penanggung Jawab", proker.penanggungJawab) %>
            <div class="md:col-span-2">
              <%= boxField("Deskripsi", proker.deskripsi) %>
            </div>
          </div>
        </section>

        <!-- =====================================================
             SECTION: TARGET PENCAPAIAN
             Menampilkan target kuantitatif dan kualitatif
             ===================================================== -->
        <section>
          <h4 class="text-lg font-semibold text-gray-700 mb-4">Target Pencapaian</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <%= boxField("Target Kuantitatif", proker.Target_Kuantitatif) %>
            <%= boxField("Target Kualitatif", proker.Target_Kualitatif) %>
          </div>
        </section>

        <!-- =====================================================
             SECTION: DOKUMEN PENDUKUNG
             Menampilkan file dokumen pendukung (PDF, gambar, dll)
             ===================================================== -->
        <section>
          <h4 class="text-lg font-semibold text-gray-700 mb-4">Dokumen Pendukung</h4>
          <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
            <% if (proker.dokumen_pendukung) { %>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <% 
                const files = proker.dokumen_pendukung.split(',');
                files.forEach((file, index) => {
                  const f = file.trim();
                  const ext = f.split('.').pop().toLowerCase();
                  const filePath = `/uploads/proker/${f}`;
                %>
                  <div class="<%= index > 0 ? 'mt-4' : '' %>">
                    <% if (['jpg','jpeg','png','gif','webp'].includes(ext)) { %>
                      <img src="<%= filePath %>" class="w-full max-h-96 object-contain rounded-lg" alt="Dokumen Pendukung"/>
                    <% } else if (ext === 'pdf') { %>
                      <embed src="<%= filePath %>" type="application/pdf" width="100%" height="500px" class="rounded-lg"/>
                    <% } else { %>
                      <div class="p-4 text-center bg-gray-50 rounded-lg">
                        <i class="fa-solid fa-file text-4xl text-gray-400 mb-2"></i>
                        <p class="text-gray-600 text-sm mb-2">File: <%= f %></p>
                      </div>
                    <% } %>
                    <a href="<%= filePath %>" target="_blank"
                      class="mt-4 inline-flex items-center gap-2 px-4 py-2 bg-orange-500 text-white rounded-xl hover:bg-orange-600 transition">
                      <i class="fa-solid fa-download"></i> Download Dokumen
                    </a>
                  </div>
                <% }) %>
              </div>
            <% } else { %>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-500 italic">Belum ada dokumen pendukung</p>
              </div>
            <% } %>
          </div>
        </section>

        <!-- =====================================================
             SECTION: TOMBOL AKSI
             Tombol untuk mengubah status program kerja
             ===================================================== -->
        <div class="pt-6 border-t">
          <% if (proker.status === "Selesai" || proker.status === "Tidak Selesai") { %>
            <!-- Info jika status sudah final -->
            <div class="p-4 bg-gray-50 border border-gray-200 rounded-xl flex items-center gap-3 mb-4">
              <div class="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm font-medium text-gray-800">Status Program Kerja Sudah Final</p>
                <p class="text-xs text-gray-600 mt-1">Status program kerja sudah final dan tidak dapat diubah kembali.</p>
              </div>
            </div>
          <% } else if (proker.status !== "Sedang Berjalan") { %>
            <!-- Info jika status belum dimulai -->
            <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-xl flex items-center gap-3 mb-4">
              <div class="w-8 h-8 bg-yellow-100 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm font-medium text-yellow-800">Program Kerja Belum Berjalan</p>
                <p class="text-xs text-yellow-600 mt-1">Aksi hanya tersedia saat program kerja sedang berjalan.</p>
              </div>
            </div>
          <% } %>

          <!-- Tombol Aksi -->
          <div class="flex gap-4 justify-end">
            <a href="/dpa/lihatProker" 
               class="px-8 py-3 border rounded-xl text-sm bg-white text-gray-700 hover:shadow-md transition">
              Kembali
            </a>

            <% if (proker.status === "Sedang Berjalan") { %>
              <button onclick="ubahStatus('<%= proker.id %>', 'Tidak Selesai')" id="btnTidakSelesai"
                      class="flex items-center gap-2 px-8 py-3 text-white bg-red-500 rounded-xl text-sm font-semibold hover:bg-red-600 transition transform hover:scale-105 active:scale-95">
                <i class="fa-solid fa-times-circle"></i>
                <span>Tidak Selesai</span>
              </button>
              
              <button onclick="ubahStatus('<%= proker.id %>', 'Selesai')" id="btnSelesai"
                      class="flex items-center gap-2 px-8 py-3 text-white bg-green-500 rounded-xl text-sm font-semibold hover:bg-green-600 transition transform hover:scale-105 active:scale-95">
                <i class="fa-solid fa-check-circle"></i>
                <span>Selesai</span>
              </button>
            <% } %>
          </div>
        </div>

      </div>
    </div>
  </main>

  <script>
    // =====================================================
    // POP-UP MESSAGE UTILITY CLASS
    // Class untuk mengelola semua modal popup di halaman
    // =====================================================
    class PopupMessage {
      static showModal(modalId) {
        const modal = document.getElementById(modalId);
        const content = document.getElementById(modalId + 'Content');
        if (!modal || !content) return;
        modal.classList.remove('hidden');
        setTimeout(() => {
          content.classList.remove('scale-95', 'opacity-0');
          content.classList.add('scale-100', 'opacity-100');
        }, 10);
      }

      static hideModal(modalId, callback = null) {
        const modal = document.getElementById(modalId);
        const content = document.getElementById(modalId + 'Content');
        if (!modal || !content) return;
        content.classList.remove('scale-100', 'opacity-100');
        content.classList.add('scale-95', 'opacity-0');
        setTimeout(() => {
          modal.classList.add('hidden');
          if (callback) callback();
        }, 300);
      }

      static showSuccess(title, message, buttonText = 'OK', callback = null) {
        document.getElementById('successModalTitle').textContent = title;
        document.getElementById('successModalMessage').textContent = message;
        const button = document.getElementById('closeSuccessModal');
        button.textContent = buttonText;
        button.replaceWith(button.cloneNode(true));
        const newButton = document.getElementById('closeSuccessModal');
        newButton.addEventListener('click', () => this.hideModal('successModal', callback));
        this.showModal('successModal');
      }

      static showError(title, message, buttonText = 'Tutup', callback = null) {
        document.getElementById('errorModalTitle').textContent = title;
        document.getElementById('errorMessage').textContent = message;
        const button = document.getElementById('closeErrorModal');
        button.textContent = buttonText;
        button.replaceWith(button.cloneNode(true));
        const newButton = document.getElementById('closeErrorModal');
        newButton.addEventListener('click', () => this.hideModal('errorModal', callback));
        this.showModal('errorModal');
      }

      static showLoading(title = 'Memproses...', message = 'Mohon tunggu sebentar.') {
        document.getElementById('loadingModalTitle').textContent = title;
        document.getElementById('loadingModalMessage').textContent = message;
        this.showModal('loadingModal');
      }

      static hideLoading(callback = null) {
        this.hideModal('loadingModal', callback);
      }
    }

    // =====================================================
    // SISTEM VALIDASI DUA TAHAP DENGAN CEK LAPORAN HMSI
    // Fungsi utama untuk mengubah status proker
    // =====================================================
    let pendingAction = null;

    async function ubahStatus(id, status) {
      // =====================================================
      // Validasi laporan HMSI sebelum ubah status menjadi 'Selesai'
      // Cek apakah masih ada laporan yang belum diterima (pending)
      // =====================================================
      if (status === 'Selesai') {
        try {
          PopupMessage.showLoading('Memeriksa Laporan...', 'Mohon tunggu, sedang memeriksa status laporan HMSI.');
          
          const checkRes = await fetch(`/dpa/proker/${id}/checkLaporan`);
          const checkData = await checkRes.json();
          
          PopupMessage.hideLoading();
          
          if (checkData.hasPending) {
            PopupMessage.showError(
              'Laporan HMSI Belum Diterima!',
              'Laporan HMSI belum ada yang diterima pada proker ini. Harap selesaikan terlebih dahulu sebelum menandai sebagai selesai.'
            );
            return;
          }
        } catch (err) {
          console.error('Error saat memeriksa laporan:', err);
          PopupMessage.hideLoading();
          PopupMessage.showError(
            'Terjadi Kesalahan!',
            'Tidak dapat memeriksa status laporan. Mohon coba lagi.'
          );
          return;
        }
      }

      // Jika tidak ada laporan pending atau status adalah 'Tidak Selesai',
      // lanjutkan ke proses konfirmasi normal
      pendingAction = { id, status };
      document.getElementById('statusTarget1').textContent = status;
      PopupMessage.showModal('confirmModal1');
    }

    // =====================================================
    // EVENT LISTENERS
    // Menangani semua interaksi user dengan modal
    // =====================================================
    document.addEventListener('DOMContentLoaded', () => {
      
      // Handler untuk tombol batal di modal konfirmasi pertama
      document.getElementById('cancelConfirm1').addEventListener('click', () => {
        PopupMessage.hideModal('confirmModal1');
        pendingAction = null;
      });

      // Handler untuk tombol lanjutkan di modal konfirmasi pertama
      document.getElementById('proceedConfirm1').addEventListener('click', () => {
        PopupMessage.hideModal('confirmModal1', () => {
          if (pendingAction) {
            document.getElementById('statusTarget2').textContent = pendingAction.status.toLowerCase();
            document.getElementById('validationInput').value = '';
            document.getElementById('validationError').classList.add('hidden');
            
            PopupMessage.showModal('confirmModal2');
            
            setTimeout(() => {
              document.getElementById('validationInput').focus();
            }, 100);
          }
        });
      });

      // Handler untuk tombol batal di modal konfirmasi kedua
      document.getElementById('cancelConfirm2').addEventListener('click', () => {
        PopupMessage.hideModal('confirmModal2');
        pendingAction = null;
      });

      // Handler untuk tombol validasi di modal konfirmasi kedua
      document.getElementById('proceedConfirm2').addEventListener('click', () => {
        const input = document.getElementById('validationInput').value;
        const errorDiv = document.getElementById('validationError');
        
        // Validasi input harus sesuai dengan teks yang diminta
        if (input !== 'YA, SAYA YAKIN') {
          errorDiv.classList.remove('hidden');
          document.getElementById('validationInput').classList.add('border-red-500');
          return;
        }
        
        // Jika validasi berhasil, lanjutkan eksekusi perubahan status
        PopupMessage.hideModal('confirmModal2', () => {
          if (pendingAction) {
            executeStatusChange(pendingAction.id, pendingAction.status);
          }
        });
      });

      // Handler untuk tekan Enter pada input validasi
      document.getElementById('validationInput').addEventListener('keypress', (e) => {
        if (e.key === 'Enter') {
          document.getElementById('proceedConfirm2').click();
        }
      });

      // Handler untuk menghilangkan pesan error saat user mengetik
      document.getElementById('validationInput').addEventListener('input', () => {
        document.getElementById('validationError').classList.add('hidden');
        document.getElementById('validationInput').classList.remove('border-red-500');
      });

      // Handler global untuk tombol Escape untuk menutup modal
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
          ['successModal', 'errorModal', 'confirmModal1', 'confirmModal2', 'loadingModal'].forEach(id => {
            const modal = document.getElementById(id);
            if (modal && !modal.classList.contains('hidden')) {
              PopupMessage.hideModal(id);
            }
          });
        }
      });
    });

    // =====================================================
    // EKSEKUSI PERUBAHAN STATUS
    // Fungsi untuk mengirim request perubahan status ke server
    // =====================================================
    async function executeStatusChange(id, status) {
      PopupMessage.showLoading('Mengubah Status...', 'Mohon tunggu sebentar.');

      try {
        const res = await fetch(`/dpa/proker/${id}/status`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ status })
        });

        const data = await res.json();

        PopupMessage.hideLoading(() => {
          if (data.success) {
            PopupMessage.showSuccess(
              'Status Berhasil Diubah!',
              data.message || `Status program kerja telah berhasil diubah menjadi "${status}".`,
              'Mengerti',
              () => window.location.reload()
            );

          } else {
            PopupMessage.showError(
              'Gagal Mengubah Status!',
              data.message || 'Terjadi kesalahan saat mengubah status.'
            );
          }
        });
      } catch(err) {
        console.error('Error detail:', err);
        PopupMessage.hideLoading(() => {
          PopupMessage.showError(
            'Terjadi Kesalahan!',
            err.message || 'Terjadi kesalahan koneksi atau server. Mohon coba lagi.'
          );
        });
      }
    }
  </script>

  <!-- =====================================================
       CUSTOM STYLES
       Animasi dan style tambahan untuk interaksi UI
       ===================================================== -->
  <style>
    @keyframes fadeIn { 
      from { opacity: 0; } 
      to { opacity: 1; } 
    }
    .animate-fade-in { 
      animation: fadeIn 0.6s ease-out forwards; 
    }
    button { 
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
    }
  </style>
</body>
</html>