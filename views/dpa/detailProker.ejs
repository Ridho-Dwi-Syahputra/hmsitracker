```html
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body class="bg-gray-50 min-h-screen flex flex-col font-sans">

  <!-- Navbar -->
  <%- include('../partials/navbarDPA', { user: user, unreadCount: unreadCount }) %>

  <!-- Sidebar -->
  <%- include('../partials/sidebarDPA', { activeNav: 'Daftar Program Kerja', user: user }) %>

  <!-- Main Wrapper -->
  <main class="lg:ml-80 px-6 pt-20 pb-10 flex-grow">
    <div class="max-w-4xl mx-auto bg-white shadow-xl rounded-2xl overflow-hidden border border-gray-200">

      <!-- Header -->
      <div class="bg-gradient-to-r from-orange-500 to-orange-600 px-6 py-6 flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold text-white">Detail Program Kerja</h1>
          <p class="text-sm text-orange-100">Informasi lengkap program kerja HMSI</p>
        </div>
        <a href="/dpa/lihatProker"
           class="flex items-center gap-2 px-4 py-2 bg-white/20 hover:bg-white/30 text-white rounded-lg transition-all duration-200">
          <i class="fa-solid fa-arrow-left text-sm"></i>
          <span class="font-medium">Kembali</span>
        </a>
      </div>

      <!-- Main Content -->
      <div class="p-6 sm:p-8 space-y-8">

        <!-- Informasi Program Kerja -->
        <section class="space-y-6">
          <h4 class="text-lg font-semibold text-gray-700 mb-2">Informasi Program Kerja</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

            <!-- Field -->
            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Nama Program Kerja</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-800 font-semibold"><%= proker.namaProker || '-' %></p>
              </div>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Divisi</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-800 font-semibold"><%= proker.divisi || '-' %></p>
              </div>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Penanggung Jawab</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-800 font-semibold"><%= proker.penanggungJawab || '-' %></p>
              </div>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Tanggal Mulai</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-800 font-semibold"><%= proker.tanggalMulaiFormatted || '-' %></p>
              </div>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Tanggal Selesai</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <p class="text-gray-800 font-semibold"><%= proker.tanggalSelesaiFormatted || '-' %></p>
              </div>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-1">Status</p>
              <div class="bg-white border border-gray-200 p-3 rounded-lg">
                <span id="status-label" class="px-3 py-1 text-sm rounded-full font-medium
                  <% if (proker.status === 'Selesai') { %> bg-green-100 text-green-700
                  <% } else if (proker.status === 'Gagal') { %> bg-red-100 text-red-700
                  <% } else { %> bg-gray-100 text-gray-600 <% } %>">
                  <%= proker.status %>
                </span>
              </div>
            </div>

            <!-- Deskripsi -->
            <div class="md:col-span-2 bg-gray-50 border border-gray-200 rounded-xl p-4">
              <p class="font-medium text-gray-600 mb-2">Deskripsi</p>
              <div class="bg-white border border-gray-200 p-4 rounded-lg">
                <p class="text-gray-800 whitespace-pre-line leading-relaxed"><%= proker.deskripsi || '-' %></p>
              </div>
            </div>

            <!-- Dokumen Pendukung -->
            <div class="md:col-span-2 bg-gray-50 border border-gray-200 rounded-xl p-4 space-y-4">
              <p class="font-medium text-gray-600 mb-2">Dokumen Pendukung</p>
              <div class="bg-white border border-gray-200 p-4 rounded-lg space-y-6">
                <% if (proker.dokumen_pendukung) { %>
                  <% proker.dokumen_pendukung.split(',').forEach(file => {
                       const f = file.trim();
                       const ext = f.split('.').pop().toLowerCase();
                  %>
                    <div class="w-full bg-gray-50 rounded-lg border border-gray-300 shadow-sm p-2">
                      <% if (['jpg','jpeg','png','gif'].includes(ext)) { %>
                        <img src="/uploads/<%= f %>" alt="<%= f %>" class="w-full h-auto rounded-lg">
                      <% } else if (ext === 'pdf') { %>
                        <iframe src="/uploads/<%= f %>" class="w-full h-96 rounded-lg" frameborder="0"></iframe>
                      <% } else if (['doc','docx','xls','xlsx','ppt','pptx'].includes(ext)) { %>
                        <iframe src="https://view.officeapps.live.com/op/embed.aspx?src=<%= encodeURIComponent('http://'+req.headers.host+'/uploads/'+f) %>"
                                class="w-full h-96 rounded-lg" frameborder="0"></iframe>
                      <% } else { %>
                        <p class="text-gray-500">File <%= f %> tidak dapat ditampilkan.</p>
                      <% } %>
                    </div>
                  <% }) %>
                <% } else { %>
                  <p class="text-gray-500 italic">Tidak ada dokumen pendukung.</p>
                <% } %>
              </div>
            </div>

          </div>
        </section>

        <!-- Tombol Ubah Status -->
        <div class="flex gap-3 justify-end mt-6">
          <% if (proker.status !== "Selesai") { %>
            <button onclick="ubahStatus('<%= proker.id %>', 'Selesai')"
                    class="px-5 py-2 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-lg shadow transition">
              ✅ Selesai
            </button>
          <% } %>
          <% if (proker.status !== "Gagal") { %>
            <button onclick="ubahStatus('<%= proker.id %>', 'Gagal')"
                    class="px-5 py-2 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-lg shadow transition">
              ❌ Gagal
            </button>
          <% } %>
        </div>

      </div>
    </div>
  </main>

  <script>
  async function ubahStatus(id, status) {
    if (!confirm(`Apakah yakin ingin mengubah status menjadi "${status}"?`)) return;

    try {
      const res = await fetch(`/dpa/proker/${id}/status`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ status })
      });
      const data = await res.json();

      if (data.success) {
        alert(data.message);

        // update label status tanpa reload
        const label = document.getElementById('status-label');
        label.textContent = status;
        label.className = "px-3 py-1 text-sm rounded-full font-medium";
        if (status === "Selesai") label.classList.add("bg-green-100","text-green-700");
        else if (status === "Gagal") label.classList.add("bg-red-100","text-red-700");

        // sembunyikan tombol yang sama
        document.querySelectorAll('button').forEach(btn => {
          if (btn.textContent.includes(status)) btn.style.display = 'none';
        });

      } else {
        alert("Gagal mengubah status: " + data.message);
      }
    } catch(err) {
      console.error(err);
      alert("Terjadi kesalahan saat mengubah status.");
    }
  }
  </script>
</body>
</html>
```
