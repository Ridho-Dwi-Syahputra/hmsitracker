<!-- Footer -->
<footer class="fixed bottom-0 bg-white border-t border-gray-200 transition-all duration-300 w-full z-40 shadow-md">
  <div class="max-w-7xl mx-auto px-6 py-4 md:flex md:items-center md:justify-between lg:pl-80">
    
    <!-- Kiri -->
    <div class="flex items-center space-x-2 text-gray-600 text-sm">
      <span>© <%= new Date().getFullYear() %> HMSI Tracker</span>
      <span class="hidden sm:inline">•</span>
      <span class="text-orange-600 font-semibold">Universitas Andalas</span>
    </div>

    <!-- Tengah (tagline) -->
    <div class="mt-2 md:mt-0 text-gray-500 text-xs italic text-center md:text-left flex-1 md:px-6">
      "Mendukung manajemen organisasi lebih mudah, transparan, dan efisien"
    </div>

    <!-- Kanan -->
    <div class="mt-2 md:mt-0 flex items-center space-x-4 text-gray-500 text-sm shrink-0">
      <span class="px-2 py-1 bg-orange-100 text-orange-700 rounded-full text-xs font-medium">
        v1.0.0
      </span>
    </div>
  </div>
</footer>
